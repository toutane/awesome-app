import React from "react";
import { View } from "react-native";

import styled from "styled-components";

export default TextInput = styled.TextInput`
  height: 50;
  border-color: rgba(0, 0, 0, 0.1);
  border-width: 1;
  border-radius: 10;
  padding-horizontal: 10;
  font-size: 18;
`;
